# TinySocs example env (copy to .env and fill values)
SIEM_BACKEND=elastic
SIEM_URL=http://localhost:9200
SIEM_USER=elastic
SIEM_PASS=changeme

LLM_MODE=openai
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_KEY= # set in your local .env only (never commit)

# Optional offline LLM toggle (kept off by default)
# LLM_MODE=ollama
# OFFLINE_LLM_URL=http://localhost:11434
# OFFLINE_LLM_MODEL=qwen2.5:0.5b-instruct
