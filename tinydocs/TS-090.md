---
rule_id: TS-090
name: service_install_suspicious
severity: high
mitre: T1543.003 (Create or Modify System Process: Windows Service)
tactic: persistence
pack: base
runs_on: agent
status: draft
---

# TS-090 — Service Install Suspicious

> Service installed with suspicious path (temp, appdata, public)

## What it detects

Event ID `7045` on channel `System`, grouped by
`winlog.event_data.ServiceName`, firing at threshold `1` within a
`5`-minute window.


_TODO: explain in plain English._

## Why it matters

_TODO: the attacker behaviour behind the signal; the "should I care at 2am" answer._

## What a true positive looks like

_TODO: concrete example of a real detection._

## Common false positives

_TODO: benign activity that also trips this, and how to allowlist it._

This rule honours these allowlist scopes (tune without editing the rule):

- `host`
- `event_data.ServiceName`

## Tuning knobs

None — single-event trigger; there is no count to tune.

## References

- MITRE ATT&CK: https://attack.mitre.org/techniques/T1543/003/
- Atomic Red Team: _TODO link `tests/atomic-tests.yaml` entry_
