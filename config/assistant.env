# TinySocs Assistant Environment Configuration
# This file is a template — the installer overwrites values from wizard inputs.
# Manual edits are preserved on upgrade (onlyifdoesntexist flag).

# ── SIEM Connection ──
# SIEM_PASS also serves as the dashboard admin password (single password model).
# Set during install or via the dashboard Settings > Change Password.
SIEM_URL=https://localhost:9201
SIEM_USER=admin
SIEM_PASS=

# ── Shared Secrets (HMAC authentication between components) ──
MASTER_SHARED_SECRET=
BOT_SHARED_SECRET=

# ── LLM Configuration ──
# LLM_MODE: anthropic | openai | offline
LLM_MODE=offline
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OFFLINE_LLM_URL=http://localhost:11434

# ── Notifications ──
# WEBHOOK_URL: generic webhook endpoint (Slack, Teams, webhook.site, etc.)
WEBHOOK_URL=
WEBHOOK_ENABLED=1

# ── Threat Intelligence (Optional) ──
# API keys for threat enrichment providers. Leave blank to disable.
# AbuseIPDB: https://www.abuseipdb.com/account/api (free: 1,000 checks/day)
ABUSEIPDB_API_KEY=
# AlienVault OTX: https://otx.alienvault.com/api (free: unlimited)
OTX_API_KEY=
# GreyNoise Community: https://www.greynoise.io/viz/signup (free: 5,000/day)
GREYNOISE_API_KEY=

# ── Dashboard Network Access ──
# DASHBOARD_BIND: 127.0.0.1 (localhost only, default) or 0.0.0.0 (network)
DASHBOARD_BIND=127.0.0.1
# DASHBOARD_TLS_CERT and DASHBOARD_TLS_KEY: paths to PEM cert/key files.
# Required when DASHBOARD_BIND=0.0.0.0. Generated by installer for network mode.
DASHBOARD_TLS_CERT=
DASHBOARD_TLS_KEY=
